//Escribe una función llamada convertirANumero que tome una cadena de texto como argumento y la convierta en un número utilizando Number(). 
//Si la conversión no es válida, debe devolver NaN.

function convertirNumero(valor){
    return Number(valor)
}

console.log(convertirNumero("143657"))