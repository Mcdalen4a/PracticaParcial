//Escribe una función llamada buscarIndice que reciba un array y un valor, y utilice indexOf() para encontrar el índice del valor en el array.

function buscarIndice(array, valor){
    return array.indexOf(valor)
}


console.log(buscarIndice([1, 2, 3, 4], 2))