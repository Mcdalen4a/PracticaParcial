//Escribe una función llamada eliminarEspacios que reciba una cadena con espacios al principio y al final, 
//y utilice trim() para eliminar esos espacios.

function eliminarEspacios(cadena){
    return cadena.trim()
}

console.log(eliminarEspacios("        2546677              "))